asset_id: SDK.WORKER.DOMAIN
name: Worker
type: Domain
domain: worker
path: conrrad_sdk/worker/

owner:
  domain: Worker
  steward: Executive Architecture

purpose:
  canonical: worker domain.

governed_by:
  - HARLEMM
  - Executive Architecture

consumed_by:
  - OMEGA
  - Executive Core

replacement:
  status: none
  successor: null

lifecycle:
  introduced: "1.0.0"
  status: ACTIVE

adr:
  - ADR-TBD

exports:
  public: true

dependencies: []

contains_glob:
  - "**/*"
