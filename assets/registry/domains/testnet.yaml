asset_id: SDK.TESTNET.DOMAIN
name: Testnet
type: Domain
domain: testnet
path: conrrad_sdk/testnet/

owner:
  domain: Testnet
  steward: Executive Architecture

purpose:
  canonical: testnet domain.

governed_by:
  - HARLEMM
  - Executive Architecture

consumed_by:
  - OMEGA
  - Executive Core

replacement:
  status: none
  successor: null

lifecycle:
  introduced: "1.0.0"
  status: ACTIVE

adr:
  - ADR-TBD

exports:
  public: true

dependencies: []

contains_glob:
  - "**/*"
