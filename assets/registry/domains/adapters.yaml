asset_id: SDK.ADAPTERS.DOMAIN
name: Adapters
type: Domain
domain: adapters
path: conrrad_sdk/adapters/

owner:
  domain: Adapters
  steward: Executive Architecture

purpose:
  canonical: adapters domain.

governed_by:
  - HARLEMM
  - Executive Architecture

consumed_by:
  - OMEGA
  - Executive Core

replacement:
  status: none
  successor: null

lifecycle:
  introduced: "1.0.0"
  status: ACTIVE

adr:
  - ADR-TBD

exports:
  public: true

dependencies: []

contains_glob:
  - "**/*"
