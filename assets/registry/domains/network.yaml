asset_id: SDK.NETWORK.DOMAIN
name: Network
type: Domain
domain: network
path: conrrad_sdk/network/

owner:
  domain: Network
  steward: Executive Architecture

purpose:
  canonical: network domain.

governed_by:
  - HARLEMM
  - Executive Architecture

consumed_by:
  - OMEGA
  - Executive Core

replacement:
  status: none
  successor: null

lifecycle:
  introduced: "1.0.0"
  status: ACTIVE

adr:
  - ADR-TBD

exports:
  public: true

dependencies: []

contains_glob:
  - "**/*"
