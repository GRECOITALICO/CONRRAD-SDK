asset_id: SDK.ROUTER.DOMAIN
name: Router
type: Domain
domain: router
path: conrrad_sdk/router/

owner:
  domain: Router
  steward: Executive Architecture

purpose:
  canonical: router domain.

governed_by:
  - HARLEMM
  - Executive Architecture

consumed_by:
  - OMEGA
  - Executive Core

replacement:
  status: none
  successor: null

lifecycle:
  introduced: "1.0.0"
  status: ACTIVE

adr:
  - ADR-TBD

exports:
  public: true

dependencies: []

contains_glob:
  - "**/*"
