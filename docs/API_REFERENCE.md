# API

Public API surface centers on the SDK Agent/CLI contracts.

```python
from conrrad import Agent
Agent().run(prompt: str) -> result
```

Infrastructure health endpoints (when deployed) are separate from the open SDK.

